{
  "name": "chasemoskal-website",
  "version": "0.0.0",
  "scripts": {
      "setup": "npm i node-sass && bower install",
      "globalSetup": "npm i -g typescript && npm i -g webpack",
      "ts": "tsc",
      "tspack": "npm run ts && webpack build/index.js build/index.bundle.js",
      "sass": "node-sass --source-map true index.scss build/index.css",
      "build": "npm run tspack & npm run sass & echo Build complete."
  },
  "devDependencies": {
    "node-sass": "^3.4.2"
  }
}
