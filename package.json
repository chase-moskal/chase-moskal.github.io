{
  "name": "chasemoskal-website",
  "version": "0.0.0",
  "scripts": {
    "install": "bower install",
    "compile-scripts": "tsc -v && tsc -p source && webpack build/main.js build/main.bundle.js",
    "compile-styles": "node-sass --source-map true source/style.scss build/style.css",
    "clean": "rimraf build",
    "build": "npm run clean && (npm run compile-scripts & npm run compile-styles) && echo Build complete.",
    "watch-styles": "node-sass --watch --source-map true source/style.scss build/style.css"
  },
  "devDependencies": {
    "node-sass": "^3.4.2",
    "rimraf": "^2.5.2",
    "typescript": "^1.8.9",
    "webpack": "^1.12.14"
  }
}
